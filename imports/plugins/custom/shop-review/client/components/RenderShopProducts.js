import React from "react";
import { Components } from "@reactioncommerce/reaction-components";

const RenderShopProducts = ({ products }) => (
  <Components.Products products={products} />
);

export default RenderShopProducts;
